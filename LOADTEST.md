# 부하테스트 결과

`Client/` 부하테스트 봇(`Client.exe [봇 수] [지속시간(초)] [서버IP] [포트]`)으로 진행한
기록. 서버·클라 모두 **Release 빌드** 기준. AWS EC2(t2.micro, Windows Server) 위에 올린
서버를 로컬 PC에서 원격으로 때리는 방식 — localhost 테스트가 아니라 실제 네트워크 왕복이
포함된 결과다.

이전에 50~1000봇까지 돌렸던 기록은 [발견한 버그](#부하테스트-중-발견한-버그) 수정 전
바이너리로 잰 결과라 지우고, 수정된 바이너리로 50봇부터 다시 잰다. RTT(핑) 통계도 이번
결과부터 같이 기록한다.

## 테스트 환경
- 서버: AWS EC2 t2.micro (1 vCPU / 1GB RAM), `IOCP_Portfolio.exe` (Release, x64)
- 클라: 로컬 PC, `Client.exe` (Release, x64)
- 대상: `15.164.95.200:9000`

## 결과 요약

| 봇 수 | 지속시간 | 총 연결 | 총 송신 | 총 수신 | 거부된 행동 | 완료된 게임 | RTT 평균 | RTT p95 | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| 50 | 60s | 50 | 4,576 | 31,028 | 0 | 236 | 9.81ms | 28.54ms | 수정된 바이너리로 재시작 후 첫 측정 |
| 100 | 60s | 100 | 5,715 | 32,006 | 0 | 234 | 8.90ms | 11.41ms | p99 36.42ms — 여전히 꼬리 김 |
| 50 (NODELAY 후) | 60s | 50 | 4,454 | 29,888 | 0 | 236 | 7.93ms | 9.93ms | p99 11.58ms — 아래 전후비교 참고 |
| 100 (NODELAY 후) | 60s | 100 | 5,608 | 31,017 | 0 | 240 | 7.60ms | 9.40ms | p99 11.04ms (이전 36.42ms) |
| 200 (NODELAY 후) | 60s | 200 | 8,036 | 34,346 | 0 | 248 | 7.74ms | 9.65ms | p99 10.70ms |
| 500 (NODELAY 후) | 60s | 500 | 15,548 | 46,419 | 0 | 268 | 7.67ms | 9.60ms | p99 10.88ms |
| 1000 (NODELAY 후) | 60s | 1000 | 27,360 | 60,084 | 0 | 296 | 8.19ms | 10.95ms | p99 23.89ms, 최대 72.95ms — 아래 참고 |
| 2000 (NODELAY 후) | 60s | 2000 | 50,922 | 86,898 | 0 | 360 | 7.72ms | 9.58ms | p99 11.14ms — 1000봇 때보다 오히려 낮음 |
| 4000 (NODELAY 후) | 60s | 4000 | 98,811 | 147,479 | 0 | 480 | 7.91ms | 9.82ms | p99 16.11ms, 최대 64.78ms |

## 다음 단계
- [x] 수정된 서버 바이너리 EC2 재배포
- [x] 50봇 60초부터 재시작
- [x] 100봇 60초 — RTT p50 7.39ms인데 p99 36.42ms로 꼬리 김 (TCP_NODELAY 미설정 확인)
- [x] 서버/클라(부하테스트 봇)/Unity 클라 전부 `TCP_NODELAY` 적용, 로컬 재검증 통과
- [x] TCP_NODELAY 적용된 바이너리 EC2 재배포 후 50봇부터 RTT p99 재측정 — p99 59.36ms → 11.58ms
- [x] 100 → 200 → 500 → 1000봇 순으로 재확인 (RTT 통계 포함)
- [x] 2000봇까지 확인 — 연결 2000 유지, 거부 0, RTT p99 11.14ms(500봇 수준으로 복귀)
- [x] 4000봇까지 확인 — 연결 4000 유지, 거부 0, RTT p99 16.11ms (1GB RAM 인스턴스가
      4000개 동시 연결을 세션당 버퍼 2KB 수준으로 별 탈 없이 받아냄)

## 1000봇 RTT 스파이크에 대한 메모
1000봇 테스트에서만 p99 23.89ms/최대 72.95ms로 튀었는데, 바로 다음인 2000봇 테스트는
p99 11.14ms로 다시 500봇 수준으로 돌아왔다. 봇 수와 함께 계속 나빠지는 추세가 아니라
1000봇 테스트에서만 튄 걸 보면, 서버가 실제로 부담을 느낀 신호라기보다 **로컬 PC가
1000개 스레드/소켓을 한꺼번에 띄우는 순간의 일시적 버스트**(부하테스트 봇 자체의 기동
비용)였을 가능성이 높다 — 실제로 1s→2s 사이 송신 카운트가 크게 튀는 구간과 겹친다.
t2.micro 한 대가 2000개 동시 연결까지도 RTT 저하 없이 버틴다는 건 이 규모의 부하테스트
로는 진짜 한계점을 못 찾았다는 뜻이기도 하다 (더 밀어붙이려면 봇을 여러 로컬 머신에
분산해서 클라이언트 쪽 병목을 먼저 제거해야 함 — 현재는 클라 자체가 병목일 가능성).
- [ ] CPU/메모리 사용률은 CloudWatch 대신 RDP 접속해서 실시간(작업 관리자)으로 확인
      (5분 평균은 60초짜리 스파이크의 정확한 피크를 못 봄)

## TCP_NODELAY 적용
50/100봇 테스트에서 RTT p50(7~8ms) 대비 p99(36~59ms)가 5~8배 튀는 패턴이 반복 확인됐다.
서버·클라(부하테스트 봇)·Unity 클라 소켓 어디에도 `TCP_NODELAY`를 설정하지 않아서
Nagle 알고리즘이 기본값(켜짐)으로 동작 중이었던 게 원인 — 게임 패킷 대부분이 작고
빈번한데, Nagle이 지연 ACK와 겹치면서 전송을 수십 ms까지 미룬 것으로 보인다. 셋 다
`setsockopt(..., TCP_NODELAY, ...)` / `TcpClient.NoDelay = true`로 켜서 고쳤다.

### 전후 비교 (50봇 60초, 동일 조건)

| 지표 | 이전 | 이후 | 개선 |
|---|---|---|---|
| 평균 | 9.81ms | 7.93ms | -19% |
| 최소 | 5.92ms | 6.11ms | 거의 동일 |
| 최대 | 71.07ms | 15.10ms | -79% |
| p50 | 7.64ms | 7.63ms | 거의 동일 |
| p95 | 28.54ms | 9.93ms | -65% |
| p99 | 59.36ms | 11.58ms | -80% |

p50은 그대로인데 p95/p99/최대가 크게 줄었다 — Nagle이 지연시키던 건 평균적인 처리
속도가 아니라 "가끔 튀는 느린 요청들"이었다는 뜻. 딱 예상한 패턴대로 나왔다.

## RTT(핑) 측정 추가
`C_Ping`/`S_Pong` 패킷을 새로 추가해서, 게임 로직과 분리된 순수 네트워크+서버 왕복시간을
잰다. 서버는 JobQueue를 거치지 않고 받는 즉시 echo한다 (DB 큐잉 지연이 안 섞이게).
봇마다 로그인 성공 후 최대 20개 표본을 모아서, 종료 시 평균/최소/최대/p50/p95/p99를
출력한다.

## 부하테스트 중 발견한 버그
로컬(localhost, RTT 0.03ms대)에서 20봇 이상으로 좀 더 오래 돌려봤더니, 몇 라운드 지나면
매칭이 완전히 멈추는 문제를 발견했다. 원인은 `OneCardRoom::EndGame()`이 GameOver를
브로드캐스트한 **뒤에** 세션의 방 참조를 지우는 순서로 되어 있어서, RTT가 거의 0인
환경에서는 클라가 GameOver를 받자마자 보낸 재입장 요청이 서버가 방 참조를 채 지우기도
전에 도착해 "이미 방에 있음"으로 잘못 거부되는 레이스였다 (거부는 응답 없이 조용히
드롭되어 클라가 영구히 대기열에서 빠짐). AWS 실네트워크(RTT 20~50ms대)에서는 1000봇
테스트까지도 이 레이스가 한 번도 안 열려서 발견되지 않았던 문제 — **부하테스트 환경을
다양화(로컬 vs 실네트워크)하는 게 왜 중요한지 보여주는 사례.** 방 참조를 브로드캐스트
전에 먼저 지우도록 순서를 바꾸고, `Session::oneCardRoom_`에도 뮤텍스를 추가해서 고쳤다
(자세한 내용은 README "개발하며 발견하고 고친 문제들" 10번 참고). 수정 후 로컬 100봇
20초 재검증 완료 — 끊김 없이 지속.
